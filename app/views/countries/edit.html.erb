<%= form_with model: @country, local: false do |f| %>
  <p>
  <div>
    <%= f.label :"Название страны" %>
    <%= f.text_field :country_name, placeholder: "Введите название" %>
  </div>
  </p>

  <div class="px-2 my-2">
    <%= f.submit "Изменить" %>
  </div>

<% end %>