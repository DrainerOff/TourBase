<%= form_with model: @city, local: false do |f| %>
  <p>
  <div>
    <%= f.label :"Название страны" %>
    <%= f.text_field :city_name, placeholder: "Введите название" %>
  </div>
  </p>


  <div class="px-2 my-2">
    <%= f.submit "Изменить" %>
  </div>

<% end %>